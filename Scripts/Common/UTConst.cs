namespace UTGame
{
    /// <summary>
    /// 一个常量类
    /// </summary>
    public class UTConst
    {
        public const int CLICK_INTERVAL_FRAME = 3; //点击间隔(帧数)，目前只用于UI

        public const int CACHE_ERROR_COUNT = 1000; //缓存池报错标记位，超过会ERROR
    }
}